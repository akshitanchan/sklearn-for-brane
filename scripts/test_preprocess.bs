import sklearn_brane;

on "worker1" {
    println("Loading dataset...");
    let data := new Data { name := "breast_cancer" };

    println("Splitting data...");
    let split := load_and_split(data, "target", "0.2");

    println("Scaling features...");
    let scaled := scale_features(split, "standard");

    println("Saved split files to /result/... plus timestamped copies");
    println("Saved scaled data and scaler to /result/... plus timestamped copies");
    println(split);
    println(scaled);
}
